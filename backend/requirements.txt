# Pinned to the versions the deploy actually runs (Render installs this file
# verbatim; floating >= ranges previously let a rebuild pull untested majors).
# Regenerate with: .venv/Scripts/pip freeze (keep only the direct deps).
openai==3.3.1
fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.4
pydantic-settings==2.15.0
python-dotenv==1.2.3
psutil==7.2.2
