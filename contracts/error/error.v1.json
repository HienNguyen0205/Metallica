{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://metallica.local/contracts/error.v1.json",
  "title": "StructuredError v1",
  "description": "Terminal and non-terminal failures share one shape. Suggested codes: bad_request, unauthorized, policy_denied, budget_exceeded, cancelled, unsupported_claim, stale_data, internal. Never store secrets in error details.",
  "type": "object",
  "required": ["message"],
  "properties": {
    "code": { "type": "string", "minLength": 1 },
    "message": { "type": "string", "minLength": 1 },
    "run_id": { "type": "string", "minLength": 1 },
    "session_id": { "type": "string", "minLength": 1 },
    "turn_id": { "type": "string", "minLength": 1 },
    "retryable": { "type": "boolean" },
    "details": { "type": "object" }
  }
}
